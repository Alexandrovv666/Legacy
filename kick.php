TRUNCATE `ban_list`;
TRUNCATE `castle`;
TRUNCATE `kast`;
TRUNCATE `lager`;
TRUNCATE `mail`;
TRUNCATE `missions`;
TRUNCATE `privilege`;
TRUNCATE `see_lager`;
TRUNCATE `session`;
TRUNCATE `speed`;
TRUNCATE `statistic_player`;
TRUNCATE `users`;